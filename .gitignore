node_modules/
.env
.env.gkclone
.phpunit.result.cache

# Built MCP server bundle. Regenerated by `npm run build` (which the
# `prepare` lifecycle script runs on install + publish), and by CI on
# every push. Keeping it out of the repo prevents drift between source
# and bundle.
dist/

# Internal planning + spec docs — kept locally, not shipped to the public repo.
docs/plans/
docs/specs/
